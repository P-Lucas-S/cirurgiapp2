import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color primary = Color(0xFF002851);
  static const Color secondary = Color(0xFF6D4C41); // Cor secundária
  static const Color error = Color(0xFFBA1A1A);
  static const Color success = Color(0xFF2E7D32); // Cor para sucesso
  // Cores de superfície
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Cores "on" (texto/ícones sobre cores)
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFFFFFFFF);
  static const Color onSurface = Color(0xFF000000);

  // Cores de borda
  static const Color outline = Color(0xFF737373);
}
